#include<stdio.h>
int main()
{
    //print an size number of the integer number and float number and double number
    int number;
    float f;
    double d;
    printf("The size of the number of integer %d\n",sizeof(number));
    printf("The size of the number of double number %d\n",sizeof(d));
    printf("The size of the number of float number %d\n",sizeof(f));








    return 0;
}
